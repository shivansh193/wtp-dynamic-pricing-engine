"""
Cash Flow Oracle - merchant settlement forecasting (Razorpay Buildathon Track 04).

Scaffold: data pipeline + GARCH/HMM/Prophet models + FastAPI /forecast endpoint.
No frontend.

The directory is `cash-flow-oracle` (hyphen); import it as `cash_flow_oracle`
via the shim below (mirrors api/_bootstrap.py for the Track 01 code).
"""
